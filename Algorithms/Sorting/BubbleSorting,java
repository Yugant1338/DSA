static int[] BubbleSorting(int[] arr){
    for(int i=0;i<arr.length;i++){
        for(int j=i+1;j<arr.length;j++){
            if(arr[i]>arr[j]){
                swap(arr[i],arr[j]); // swap method is used here to interchange there values
            }
        }
    }
    return arr;
}
// swap mathod is created here 
static void swap(int a, int b) {
        int temp = a;
        a = b;
        b= temp;
}
